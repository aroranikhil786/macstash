id: vscode
name: Visual Studio Code
category: editor
detect:
  app: /Applications/Visual Studio Code.app
  brew_cask: visual-studio-code
capture:
  paths:
    - path: ~/Library/Application Support/Code/User/settings.json
      class: public
    - path: ~/Library/Application Support/Code/User/keybindings.json
      class: public
    - path: ~/Library/Application Support/Code/User/snippets
      class: public
restore:
  note: |
    Extensions are captured as a list of ids and reinstalled during restore with
    `code --install-extension`. Neither end needs that command on your PATH: the
    list is read from the extensions manifest VS Code maintains, and the command
    is found inside the application itself. If VS Code is not installed here yet,
    the ids are printed instead so you can install them once it is.
    Saved credentials live in globalStorage, which is never captured.
